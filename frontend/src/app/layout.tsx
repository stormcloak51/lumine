import type { Metadata } from 'next'
import './globals.css'

import '@mantine/core/styles.css'

import { ColorSchemeScript, MantineProvider } from '@mantine/core'
import { Header } from './components/Header'
import * as font from '../fonts/fonts'
import { ThemeProvider } from 'next-themes'
import SideNav from './components/SideNav'
import ClientProvider from './ClientProvider'

export const metadata: Metadata = {
	title: 'lumine',
	description: 'Generated by create next app',
}

const isAuth = false

export default function RootLayout({
	children,
	isUserAuth = isAuth,
}: Readonly<{
	children: React.ReactNode
	isUserAuth: boolean
}>) {
	console.log('LOGGER', children)
	return (
		<html lang='en'>
			<head>
				<ColorSchemeScript />
			</head>
			<body
				className={`${font.geistSans.variable} ${font.geistMono.variable} ${font.helvetica_bold.variable} ${font.helvetica_regular.variable} antialiased mx-auto ${isAuth ? 'max-w-[1224px]' : ''}`}>
				<ClientProvider>
					<ThemeProvider defaultTheme='dark'>
						<MantineProvider defaultColorScheme='dark'>
							
							{isUserAuth && <Header />}
							<main className={isAuth ? 'pt-[120px]' : ''}>
								{isUserAuth && <SideNav />}
								{children}
							</main>
						</MantineProvider>
					</ThemeProvider>
				</ClientProvider>
			</body>
		</html>
	)
}
